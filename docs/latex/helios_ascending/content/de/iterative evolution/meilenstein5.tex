\subsection{Meilenstein 5: ECS und UI-Architektur}


Für Meilenstein 5 war vorgesehen, die Enginesysteme mit dem umzusetzenden Gameplay zu verzahnen.
Da die fundamentalen Enginesysteme zu diesem Zeitpunkt bereits eine stabile Struktur aufwiesen, wurde zunächst die Datenhaltung der Spielobjekte innerhalb der GameWorld auf eine idiomatischere ECS-Architektur umgestellt.
Dabei beschränkten sich die notwendigen Änderungen weitgehend auf den Datenhaltungs-Unterbau; die APIs von \textit{GameWorld} und \textit{GameObject} konnten durch die Kapselung der Datenverwaltung weitestgehend unverändert bleiben (Zitat Information Hiding Parnas).
Der größte Migrationsaufwand entstand durch den Austausch der bislang in \textit{helios} verwendeten GUIDs zur Identifikation von Spielobjekten.\footnote{Eine GUID ist eine global eindeutige Identifikationsnummer, die intern als \texttt{uint\_64\_t} implementiert ist.}\\

Mit dem Meilenstein wurde außerdem die Trennung von Interface und Implementation Units aufgehoben: Mit dem für einen iterativen Entwicklungsprozess typischen Refactoring-Aufwand ergab sich trotz ausreichender IDE-Unterstützung (CLion) eine spürbare kognitive Last durch die häufigen Kontextwechsel bei der Navigation durch die Quelldateien, was Verzögerung bei der konsistenten Anpassung von Schnittstellendefinition und zugehöriger Implementierung begünstigte.
Die Auswirkungen auf die Kompilierzeit wurden als vernachlässigbar eingestuft.\\

Mit zunehmender Erfahrung mit C++ in diesem Projekt wuchs auch das Vertrauen in die eigene Codebasis.
Die ursprünglich konsequente Verwendung von \texttt{unique\_ptr} und \texttt{shared\_ptr} zur Abbildung eindeutiger Besitzverhältnissen und Move-Smantik wurde in den tieferen Schichten der Engine schrittweise durch rohe Zeiger und Referenzen ersetzt.
Vorkommen von Referenzen als Übergabeparameter sollen als Schnittstellendefinition eine an anderer Stelle vereinbarte Lebenszeitgarantie liefern, die in höheren Schichten durch \texttt{unique\_ptr} ausgedrückt wird.\\

Mit Umstellung des \textit{Mental Models} auf \textit{composition over inheritance} wurde zudem die Template-Metaprogrammierung als idiomatischer Ansatz zur Bereitstellung generischer Komponenten und Systemen etabliert.
Dies rmöglicht einen weitgehenden Verzicht auf Vererbung, virtuelle Funktionen und den damit verbundenen Laufzeitkosten.
Ganz ohne Basisklassen kommt helios nicht aus.
Diese beschränken sich jedoch überwiegend auf den Einsatz bei dem Type-Erasure-Idiom und bleiben in der Regel in einer flachen Vererbungshierarchie.
Eine Ausnahme bilden spezialisierte Manager, die von \texttt{Manager} erben und durch die gleichzeitige Nutzung als \texttt{CommandHandler} die Schnittstelle mit der von \texttt{TypedCommandHandler<T>} kombinieren.
Da Manager jedoch vergleichsweise selten in der GameLoop geflusht werden (hierzu sind individuell konfigurierbare \texttt{CommitPoints} sowie \textit{Phasenübergängen} vorgesehen), bewerten wir den Overhead durch die VTable-Lookups an dieser Stelle nicht als kritisch, wobei eine belastbare Messung aussteht.\\

In Kombination mit Type-Erasure, das die Ablage spezialisierter Typen in einem als type-erased Vektor erlaubt, nutzen wir außerdem einen \texttt{TypeIndex}.
Dieser wird mit einem \texttt{struct} initialisiert und erzeugt auf Basis der übergebenen Typen einen dichten Index.
In entsprechenden Anwendungsfällen ermöglicht dies einen elementweisen Zugriff in $O(1)$, während sich dichte (\textit{dense}) Speicherlayouts, die eine Iteration in $O(N)$ erlauben.
In den meisten Fällen entspricht $N$ der tatsächlichen Elementanzahl $M$, und nur in seltenen Fällen ist aufgrund der \textit{sparsity} $N > M$.




Dies stellt nur einen kleinen Ausschnitt der Änderungen in der Datenhaltung dar, die mit Meilenstein 5 eingenagen sind.
Auf einige Aspekte werden wir im späteren Verlauf noch näher eingehen, wie bspw den graduellen Verzicht auf Unique und shared ptr, was durch eine vertrauteren Umgang mit C++ durch die längere Lernphase bergründet ist (mehr Selbstverrtrauen, das nichts falsch ist).\\

Mi den Änderungen der Codebasis musste nun auch eine UI her, damit das Spiel dem Anwender die Möglichkeit bietet, von einem Titel-Scrren hin zum Start des Spiel zu kommen, das Spiel zu pausieren, und von dort woieeraufzunehmen, neuzustarten oder zu beenden.\\

Die Implementierung dieser Funktionen erwies sich als aufwändig, da wir zu dieser späten Phase des Projektes aus dem Gedankenmodells des Spiels Und Frame N+1 heraus mussten und uns über die Implementierung von UI Gedanken machen mussten, was letztendlich alelrdings auch über das ECS und Compoenents passiert sowiue der in den vorherigen Abschnitten angesprochenen Viewports, die mit orthografischer Projektion die UI Scenen  direkt über die Gameworld im Renderpass rendern.\\

Für die Umsetzung der Texte wurde die Bibliothek \textit{freetype} eingebunden, um die benötigten Glyphen zu rendern, wo bei wir der Implementierung der Einfachheit halber deVires folgen. Ein einfaches Layouting-System, das als ECS-System in die GameLoop eingebunden wird, übernimmt die Berechnung der Position von UI Elementen.


\begin{lstlisting}[style=c++style,
caption={Implementierung eines einfachen Score-Textes, der die Punktzahl des Spielers anzeigt. Die UI-Elemente werden genau wie aktive, mit der Spielwelt verbundene Objekte betrachtet und entsprechend konfiguriert.},
label=lst:gameobjectdsl]
    auto scoreText = GameObjectFactory::instance()
        .gameObject(gameWorld)
        .withRendering([&](auto& rnb) {
            rnb.textRenderable()
               .text("0")
               .formattedAsNumber()
               .fontId(uiTextFont)
               .fontScale(1.0f)
               .fontResourceProvider(renderingDevice.fontResourceProvider())
               .shader(glyphShader)
               .color(helios::util::Colors::WhiteSmoke)
               .attachTo(&hudScene.root());
    })
    .withObserver([&](auto& ob) {
        ob.observe()
          .runningScore(ScorePoolId{"playerOneScorePool"});
    })
    .withUiTransform([](auto& tb) {
        tb.transform()
          .pivot(Anchor::TopRight)
          .viewport(ViewportId{"hudViewport"})
          .anchor(Anchor::TopRight)
          .offsets({60.0f, 50.0f, 0.0f, 0.0f});
    })
    .make();
\end{lstlisting}


Zur Steuerung der Spiels in in den verschiedenen Zuständen wurde außerdem ein einfaches, konfiguroerbares State Machine System auf Basis von templates implmentiert, was es erlaubt, eigene States und StateTransitions zu definieren.
Als Default Implementierung liefert helios GameState udn MatchState, wobei GameState die Shell repräsentiert und allgemeine Systemzustände wie \texttt{Title}, \texttt{Running} und \texttt{Paused} definiert, und sich MatchState sich auf Logik eines einzelnen Spieldurchlaufs konzentiert mit Zuständen wie \texttt{Warmup}, \texttt{Playing} und \texttt{GameOver}.\\

Eine Anpassung der nunmehr komplexeren GameLoop, die eine Unterteilugn in verschiedene Phasen \texttt{Pre}, \texttt{Main} und \texttt{Post} zur einfacheren Verortung von Spiellogik, Physik-Simulation, Kollisionserkennung und  Abgleich der Spielwelt mit dem Szenegraphn  erlaubt, bekam zusätzlich noch eine Anbsingund an die State Machnine, wodurch es möglich ist, pber einfache Angabe deer States als Parameter zu definieren, in welcher Phase ein System laufen soll.

\begin{figure}[H]
\centering
\includegraphics[width=0.8\columnwidth]{content/de/iterative evolution/img/scoringdemo_title}
\caption{Titelbildschirm der Scoring-Demo, die mit Meilenstein 5 veröffentlich wurde. (Quelle: eigene Aufnahme)}
\label{fig:scoringdemo_title}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.8\columnwidth]{content/de/iterative evolution/img/scoringdemo_playing}
\caption{Die Scoring-Demo enthält die wesentlichen für diesen Meilenstein festgesetzten Anforderungen, darunter Highscore-Anzeige sowie ein zeitbegrenztes Level. Dargestellt ist im UI außerdem die Anzeige der zur verfügbar stehenden Leben. (Quelle: eigene Aufnahme)}
\label{fig:scoringdemo_running}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.8\columnwidth]{content/de/iterative evolution/img/scoringdemo_pause}
\caption{Pause-Menü der Scoring-Demo. Von hier kann wieder in den Titelbildschirm gewechselt, das Spiel beendet oder entsprechend fortgesetzt werden. (Quelle: eigene Aufnahme)}
\label{fig:scoringdemo_pause}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.8\columnwidth]{content/de/iterative evolution/img/scoringdemo_gameover}
\caption{Gameover-Screen der Scoring-Demo. (Quelle: eigene Aufnahme)}
\label{fig:scoringdemo_gameover}
\end{figure}
