\section{ECS als relationales Modell}

Im folgenden Abschnitt motivieren wir die idiomatische ECS-Datenstruktur auf Grundlage des relationalen Modells.\\
Mit Hilfe von funktionalen Abhängigkeiten weisen wir die Normalformen nach, um im Weiteren mit Normalformtheorien zusammenhängende Lösch- und Aktualisierungsanomalien formal ausschließen zu können.\\
Ausgehend von einem breiten Relationsschema überführen wir den Entwurf in ein ECS-idiomatisches komponentenzentrisches Modell und weisen hierfür die 6. Normalform nach.

\subsubsection*{Hinweise zur Notation}
Bei Begriffen und Notation folgen wir der üblichen Fachliteratur, insbesondere Elmasri und Navathe~\cite{EN11}.\\
Mit Blick auf die Relationen zwischen Entität und Komponenten bezeichnen wir das relationale Modell des ECS im Folgenden als \textit{ECS-Modell}.
Wo sich die Erweiterungen des Modells nicht aus dem Kontext ergeben, wird entsprechend erklärend darauf hingewiesen.\\
Um der übrigen Notation in dieser Arbeit gerecht zu bleiben, verwenden wir auch in diesem Abschnitt für die Subskripte der Komponententypen Buchstaben.
Wo der Zusammenhang erfordert, ersetzen wir die Buchstaben durch Ziffern, um beispielsweise deutlicher auf eine endliche Menge von Komponentenassoziationen hinzuweisen; diese setzen wir aber stets -- unabhängig von der Schreibweise -- implizit voraus.\\
\medskip

\noindent
Folgenden Abkürzungen werden verwendet: \textit{FD} bezeichnet \textit{Funktionale Abhängigkeit}, \textit{MVD} bezeichnet \textit{Mehrwertige Abhängigkeit}, \textit{JD} bezeichnet
\textit{Join Abhängigkeit}. \textit{NF} steht als Abkürzung für \textit{Normalform}.
Die Verwendung von Plural beziehungsweise Singular ergibt sich aus dem jeweiligen Kontext.\\

\noindent
Für Relation verwenden wir im Folgenden Großbuchstaben, beispielsweise $R$.\\

\noindent
Für den Relationszustand einer Relation $R$ notieren wir $r(R)$, oder einfach $r$, falls sich aus dem Kontext ergibt, welche Relation gemeint ist.\\

\noindent
Wir notieren $\pi_{R_i}(r)$ für die Projektion des Relationszustands $r(R)$ auf die Attributmenge der Relation $R_i \subseteq R$.\\

\noindent
Der binäre Natural-Join-Operator $\njoin$ wird im Kontext von Relationszuständen $\pi_{R_i}(r)$ in der Form
\[
    \njoin (\pi_{R_1}(r), \pi_{R_2}(r), \ldots, \pi_{R_n}(r)),\ n \in \mathbb{N}
\]
\noindent
genutzt und ersetzt damit die Schreibweise
\[
    \pi_{R_1}(r) \njoin \pi_{R_{2}}(r) \njoin \ldots \njoin \pi_{R_n}(r)
\]
\medskip

\noindent
Im Folgenden schließt \textit{verlustfrei} im Zusammenhang mit Joins und Zerlegung auch immer die Eigenschaft \textit{nicht-additiv} mit ein.\\

\noindent
Wir überladen die aus der Arbeit von Redmond et al. genutzten Bezeichner $K_i$ für das ECS-Modell.
Hier bezeichnen wir mit $K_i$ -- in Anlehnung an den \textit{Typ} -- eine Relation mit der Attributmenge $\{e, k_i\}$.

\[
    K_i(e, k_i)
\]

\noindent
Der Wertebereich $\text{dom}(k_i)$ entspricht dem Wertebereich der darzustellenden Komponente, beziehungsweise den der Implementierung der darunterliegenden Programmiersprache (Redmond et al.: dem \textit{Typ} $K_i$).\\

\noindent
Damit wollen wir die Typsemantik aus der zitierten Arbeit auf einen relationalen Kontext übertragen, also beispielsweise

\[
    c \coloneq \{c_i: E \rightharpoonup K_i\}_{i \in I}
\]
\medskip

\noindent
auf
\[
    c_i \mathrel{\widehat{=}} r(K_i)
\]

\noindent
sowie $c$ auf
\[
    c \mathrel{\widehat{=}} \{r(K_i)\}_{i \in I}
\]
\medskip



\subsection{Das relationale Schema}
Ein erster Entwurf eines relationalen Schemas, das die Beziehungen zwischen \textit{Entität} und \textit{Komponenten} ausdrückt, führt zu dem in Abbildung~\ref{fig:relation} dargestellten Modell.
Der Entitätstyp $E$ ist dort mit Komponententypen $K_1$, $K_2, \ldots$ dargestellt, wobei die Beziehungen als binäre Relationen modelliert sind. Der Ansatz wird bei uns als \textit{entitätszentrischer Entwurf} bezeichnet und in ähnlicher Form bei Redmond et {al.} verwendet~\cite[Fig.1, 272:3]{RCCM+25}.


\begin{figure}[!htbp]
    \centering
    \includegraphics[width=0.6\columnwidth]{content/de/appendix/concurrency_paper/notation/img/relation}
    \caption{Entwurf eines entitätszentrischen Entity-Relationship-Modells für Entitäten und assoziierte Komponenten.
    Relationen sind identifizierend modelliert, da ein Komponententerm $k_m, m \in \mathbb{N}$ nicht ohne eine besitzende Entität existieren kann.
    Entsprechend sind $K_1, K_2, \ldots$ als schwache Typen modelliert, die über $E$ identifiziert werden. Die Notation folgt der Chen-Notation~\cite{Che76}. (Quelle: eigene Darstellung)}
    \label{fig:relation}
\end{figure}

\subsection{Funktionale Abhängigkeiten im ECS-Modell}

Relationen und Attributmengen des ECS-Modells ergeben sich wie folgt:

\[
    \begin{alignat}{3}
        &E   &&= \{e\}\\
        &K_1 &&= \{e, k_1\}\\
        &K_2 &&= \{e, k_2\}\\
        &K_3 &&= \{e, k_3\}\\
        & \ldots
    \end{alignat}
\]

\noindent
Als Primärschlüssel für $E$ identifizieren wir das Attribut $e$.
In den Relationen $K_1, K_2, \ldots$ ist $e$ der Fremdschlüssel, und aufgrund der 1:1-Kardinalität der Relationen auch gleichzeitig ihr Primärschlüssel.\\

\noindent
Die funktionalen Abhängigkeiten der Relationen lauten:

\[
    \begin{alignat}{3}
        F_E = &\{ e \rightarrow e\}\\
        F_{K_1} = &\{ e \rightarrow k_1\}\\
        F_{K_2} = &\{ e \rightarrow k_2\}\\
        \ldots
    \end{alignat}\
\]
\medskip

\noindent
Entitäten, die eine oder mehrere garantierte Assoziationen zu Komponenten $K_1, K_2, \ldots, K_n$ besitzen, können in ein breites Relationsschema überführt werden (siehe Abbildung~\ref{fig:breiterelation_k}):
\[
    K_J(e, k_1, k_2, \ldots, k_n),\quad J \subseteq I
\]


\begin{figure}[!htbp]
    \centering
    \includegraphics[width=0.6\columnwidth]{content/de/appendix/concurrency_paper/notation/img/breiterelation_k}
    \caption{Modellierung von Komponenten und Entitäten, zusammengeführt in der Relation $K_J$. (Quelle: eigene Darstellung)}
    \label{fig:breiterelation_k}
\end{figure}

\noindent
Die funktionalen Abhängigkeiten $F_{K_{J}}$ von $K_{J}$ ergeben sich damit zu

\[
    \begin{alignat}{3}
         F_{K_J} &= F_E \cup F_{K_1} \cup F_{K_2} \cup \ldots =
            &\{e \rightarrow e, e \rightarrow k_1, \ e \rightarrow k_2, \ \ldots\}
    \end{alignat}
\]
\medskip

\noindent
Die Hülle $e^+$ von $e$ unter $F_{K_J}$ lautet:

\[
    e^+ =\{e, k_1, k_2, \ldots, k_n\}
\]

\noindent
Folglich ergibt sich die minimale funktionale Abhängigkeitsmenge $F^\text{min}_{K_J}$ zu:
\[
    \begin{alignat}{3}
        F^\text{min}_{K_J} = &\{e \rightarrow {k_1},\ e \rightarrow {k_2}, \ \ldots \ e \rightarrow {k_n}  \}
    \end{alignat}
\]

\noindent
Daraus folgt
\[
    \begin{alignat}{3}
        (F^\text{min}_{K_J})^+ = (F_{K_J})^+
    \end{alignat}
\]


\subsection*{$K_J$ zur Entitätsdarstellung}

Redmond et al. führen den Unit-Wert $*$ vom Typ $\top$ ein.
Damit kann im Sinne der Typsemantik formuliert werden, dass über eine Anfrage wie etwa

\[
    \llparenthesis \tm{anyway}_i \rrparenthesis = K_i + \top
\]

\noindent
nicht nur der Typ der angefragten Komponenten $K_i$ ausgedrückt wird, sondern auch der Typ $\top$, falls die Komponente $K_i$ nicht für die Entität definiert ist.\footnote{$K_i$ ist in diesem Zusammenhang der Komponententyp in der dem \textit{ECS Core}-Modell zugrundeliegenden Programmiersprache, vgl. Redmond et al.: ``Each $K_i$ is a type in the sub-language, [\dots]''.}\\

Im relationalen Modell ist die Attributmenge hingegen über die Schemadefinition bekannt.
Ein Zusammenfassen aller möglichen Komponentenattribute in einer breiten Relation ist allerdings dann nicht möglich, wenn Tupel die in \textit{ECS Core} eingeführte partielle Abbildung
\[
    c_i : E \rightharpoonup K_i
\]
repräsentieren sollen: Offensichtlich beinhaltet $r(K_J)$ nur solche Tupel, die Entitätsterm $e$ sowie Komponententerme $k_1, k_2, \ldots , k_n$ besitzen:
Es gilt:
\[
    \forall t \in r(K_J),\forall k_i \in K_J \setminus \{e\}: t[k_i] \in \text{dom}(k_i)
\]
\smallskip

\noindent
Damit folgt, dass eine Entität, die eine Assoziation zu $K_\text{Position}$ hat, aber keine Assoziation zu $K_\text{Velocity}$, nicht Teil des Relationszustands $\pi_{K_J}(r)$ sein kann, sofern $k_\text{Velocity}$ Attribut der Relation $K_J$ ist.\\
Eine Totalrelation $K_\text{Total}$, die alle möglichen Komponentenattribute enthält und $\text{live}(c)$ repräsentiert, ist damit ohne Anpassung des relationalen Modells nicht möglich.
 Die Einführung eines \texttt{NULL}-Wertes oder die Erweiterung des Wertebereichs der Komponentenattribute um

\[
    \text{dom}(k_i)^\bot = \text{dom}(k_i) \cup \{\bot\}
\]
\noindent
würden das ECS-Modell um eine notwendige Wertsemantik erweitern, über die die Abwesenheit einer Komponente ausgedrückt werden kann.
 Nachteile, die sich durch die Einführung von \texttt{NULL}-Werten ergeben, werden in der Literatur ausführlich diskutiert und sollen hier nicht noch einmal aufgegriffen werden.\footnote{
    Elmasri und Navathe in~\cite[S. 306--307]{EN11}; ``Dreiwertige Logik`` wird von Date in \cite[S. 22--29]{Dat95} diskutiert, ausführlich außerdem in \cite[S. 575--600]{Dat03}.
}

Die Einführung eines speziellen $\bot$-Wertes, der die Erweiterung des Wertebereichs erforderlich macht\footnote{
    Diskussionen hierzu in \cite[S. 186--221, S. 229--236]{DDM98}.
}, würde spätestens bei Anfrage sowie Zerlegung von $K_J$ zu Schwierigkeiten führen:

Würde das relationale Modell einen Spezialwert $\bot$ für jeden Komponententyp $K_i$ besitzen, müssten wir diesen zusätzlich in den Auswertungen von Anfragen berücksichtigen.
Komponenten-Assoziationen müssten dann auf Grundlage des $\bot$-Wertes interpretiert werden, da $\bot$ die Abwesenheit einer Komponente ausdrückt.\footnote{
    Zusätzliche Integritätsbedingung würden in dem Fall ausschließen können, dass ein Tupel ausschließlich $\bot$-Werte für die Komponentenattribute aufweisen darf: $\forall t \in r(K_J): \exists k_i \in K_J \setminus \{e\}: t[k_i] \neq \bot$
}
\medskip
Selektionen müssten so formuliert werden, um die Semantik der partiellen Abbildung

\[
    E \rightharpoonup K_i
\]
\noindent
 ausdrücken zu können, beispielsweise als Selektion und nachfolgende Projektion:
 ausdrücken zu können, beispielsweise als Selektion und nachfolgende Projktion:

 \[
     \pi_{e, k_\text{Position}, k_\text{Velocity}}(\sigma_{k_\text{Position} \neq \bot \land k_\text{Velocity} \neq \bot}(K_J))
 \]


\bigskip
 Die Semantik von \textit{ECS Core} würde damit im relationalen Modell über eine Erweiterung des Wertebereichs sowie eine inhaltliche Interpretation der Anfrageergebnisse umgesetzt, während die zitierte Arbeit die Konjunktion der Anfrage-Bedingungen als relationalen Join versteht.\\

Aus den genannten Gründen verzichten wir auf eine Totalrelation mit Hilfe von \texttt{NULL}- beziehungsweise Spezialwerten, und modellieren das ECS-Modell als Familie von Relationen, die jeweils nur tatsächliche Komponentenassoziationen abbilden.
Damit gehen wir von folgendem aus: Zu $k_1, k_2, \ldots, k_n$ im ECS-Modell können zu jedem Zeitpunkt Relationen $K_J$ mit beliebiger nichtleerer Teilmenge dieser Komponentenattribute existieren.

Sei hierbei
\[
    \mathcal{K}_P = P(\{k_1, k_2, \ldots, k_n\}) \setminus \{\emptyset\}
\]

\noindent
die Potenzmenge über alle möglichen Komponentenattribute, dann ist $K_J$ eine Relation mit

\[
    K_J = \{e\} \cup R, \text{ mit } R  \in \mathcal{K}_P
\]

\noindent
\textbf{Beispiel}:\\
Seien $K_{J_1} = \{e, k_1, k_2\}, K_{J_2} = \{e, k_2, k_5\}$ mit $t_1 \in r(K_{J_1}), t_2 \in r(K_{J_2})$, und gelte

\[
     t_1[e] = t_2[e]
\]

\noindent
Dann repräsentiert das Tupel

\[
   t_e \in r(K_{J_1}) \njoin r(K_{J_2})
\]

\noindent
eine Entität, die über Komponentenassoziationen $k_1, k_2, k_5$ verfügt.\\

Im Weiteren verzichten wir auf eine Indizierung von $K_J$ -- für die Nachweise der Normalformen ist nur der Aufbau einer solchen Relation von Bedeutung:

\[
    K_J = \{e\} \cup \{k_j \mid j \in J \}
\]

Die nachfolgenden Abschnitte motivieren auf dieser Grundlage die Einführung der \textit{komponentenzentrischen Relationen} von der Form $K_i(e, k_i)$.

\subsection*{Nachweis der Normalformen}

Wir führen im folgenden den Nachweis der 1., 2., 3., 4., und 5. sowie der Boyce-Codd-Normalform für $K_J$.
Wie beginnen zunächst mit einigen Hilfssätzen, die den Nachweis erleichtern.\\


\bigskip
\begin{axiom}\label{ax:nur_atomar}
    Die Wertemengen der Attribute von $K_J$ lassen nur einzelne, untrennbare (und damit atomare) Werte zu.
\end{axiom}

\bigskip
\begin{axiom}\label{ax:einfacher_schluessel}
    Der einzige Schlüsselkandidat der Relation $K_J$ ist $e$.
\end{axiom}
\bigskip

\begin{definition}\label{def:entitätsbezogen}

    Sei
    \[
        K_J = \{e\} \cup \{k_j \mid j \in J\}
    \]
    ein Relationsschema über eine endliche Komponentenattributmenge $\{k_1, k_2, \ldots, k_n\}$.\\

    \noindent
    Ein Teilschema $R \subseteq K_J$ heißt \textit{enitättsbezogen}, wenn

    \[
        e \in R
    \]
    gilt.\\

    \noindent
    Eine Zerlegung

    \[
        D = (R_1, R_2, \ldots, R_m)
    \]
    von $K_J$ mit

    \[
        R_1 \cup R_2 \cup \ldots \cup R_m = K_J
    \]
    heißt \textit{entitätsbezogene Zerlegung}, wenn jedes Teilschema $R_\ell \in D$ entitätsbezogen ist, also
    \[
        \forall R_{\ell} \in D: e \in R_{\ell}
    \]
    gilt.\\

    \noindent
    Insbesondere kann jedes Teilschema $R_\ell$ als
    \[
        K_{J_{\ell}} = \{e\} \cup \{k_j \mid j \in J_\ell\}, \quad J_{\ell} \subseteq J
    \]
    geschrieben werden
\end{definition}


\bigskip
\begin{lemma}\label{lem:K_bin_ist_verlustfrei}
    Sei \(J = \{i,j\}\), \(J_i = \{i\}\) und \(J_j = \{j\}\).
    Die entitätsbezogene Relation
    \[
        K_J = K_{\{i,j\}}(e, k_i, k_j)
    \]
    lässt sich verlustfrei in die entitätsbezogenen Relationen
    \[
        K_{J_i}(e, k_i),\quad K_{J_j}(e, k_j)
    \]
    zerlegen.
\end{lemma}

\medskip
\begin{proof}
    Sei
    \[
        K_{J_i} = \pi_{(e,k_i)}(K_J),
        \qquad
        K_{J_j} = \pi_{(e,k_j)}(K_J).
    \]

    \noindent
    Wir zeigen, dass
    \[
        \pi_{K_{J_i}}(r)\
        \bowtie_{K_{J_i}.e = K_{J_j}.e}\
        \pi_{K_{J_j}}(r)
        =
        \njoin\bigl(\pi_{K_{J_i}}(r), \pi_{K_{J_j}}(r)\bigr)
        =
        \pi_{K_J}(r).
    \]

    \bigskip
    \noindent
    Da es sich um eine binäre Zerlegung handelt, kann der Beweis anhand der funktionalen Eigenschaften erfolgen~\cite[S. 343]{EN11}:
    \[
        \begin{alignedat}{3}
            ((K_{J_i} \cap K_{J_j}) &\rightarrow (K_{J_i} - K_{J_j}))
            = (e \rightarrow k_i) \in F_{K_J}^+\\
            (K_{J_i} \cap K_{J_j}) &\rightarrow (K_{J_j} - K_{J_i}))
            = (e \rightarrow k_j) \in F_{K_J}^+
        \end{alignedat}
    \]
\end{proof}


\bigskip
\begin{lemma}\label{lem:K_plus_ist_verlustfrei}
    Die Relation $K_J = K(e, k_1, k_2, \ldots, k_n), n \geq 2$ lässt sich verlustfrei entitätsbezogen nach $D= (K_{J_1}, K_{J_2}, \ldots, K_{J_m}, m \geq 2)$ zerlegen.
\end{lemma}

\medskip
\begin{proof}
    Um Lemma~\ref{lem:K_bin_ist_verlustfrei} auf m-äre Zerlegungen $D_m = (K_{J_1}, K_{J_2}, \ldots, K_{J_m}, m \geq 2)$ von $K_J$ auszuweiten, ist zu zeigen:
    \[
        \pi_{{K_J}}(r) =\ \njoin (\pi_{K_{J_1}}(r), \pi_{K_{J_2}}(r), \ldots, \pi_{K_{J_m}}(r))
    \]

    Hierzu wenden wir das Chase-Verfahren an~\cite{ABU79}~\cite[Algorithmus 10.2, S. 342]{EN11}.
    Für eine bessere Lesbarkeit verzichten wir in der Tableaudarstellung auf die $b$-Symbole und nutzen stattdessen Unterstriche $\_$ in entsprechenden Zellen.\\

    \noindent
    Die Zerlegung der einzelnen Relationen erfolgt zunächst entitätsbezogen nach Definition~\ref{def:entitätsbezogen} , so dass
    \[
        \forall K_{J_i} \in D : e \in K_{J_i},\quad \lvert K_{J_i} \rvert \geq 2
    \]
    Damit ist sichergestellt, dass der Primärschlüssel von $K_J$ Attribut jeder Relation ist.

    \noindent
    Sei zunächst wie in dem Verfahren angegeben das Tableau wie folgt aufgebaut:
    Spalten entsprechend den Attributnamen von $K_J$, in der Reihenfolge $e, k_1, \ldots, k_n$.
    Die Zeilen sind mit den Relationsnamen $K_{J_1}, K_{J_2}, \ldots, K_{J_m}$ der Zerlegung markiert.
    Wir erhalten dadurch folgendes Tableau, wobei $\circ$ als Platzhalter für die jeweiligen Zelleninhalte steht:\\

    \[
        \begin{array}{c|cccccc}
            & e      & k_1 & k_2 & k_3 & \dots  & k_n \\
            \hline
            K_{J_1}      & \circ     & \circ         & \circ         & \circ         & \dots  & \circ         \\
            K_{J_2}      & \circ     & \circ         & \circ         & \circ         & \dots  & \circ         \\
            K_{J_3}      & \circ     & \circ         & \circ         & \circ         & \dots  & \circ         \\
            \vdots          & \vdots & \vdots     & \vdots     & \vdots     & \ddots & \vdots     \\
            K_{J_m}      & \circ     & \circ         & \circ         & \circ         & \dots  & \circ
        \end{array}
    \]
    \medskip

    Entsprechend der Vorschrift des Verfahrens gilt in unserem Fall, dass für jede Relation, die in das Tableau übernommen wird, die Spalte $e$ der neu hinzugefügten Zeile mit dem Symbol $a_e$ besetzt wird: $e$ ist Attribut und Schlüssel jeder Relation $K_{J_y}, y \leq m$ und taucht dementsprechend in der linken Seite aller FDs auf:
    \[
        \forall (X \rightarrow A) \in F^\text{min}_{K_J}: X = e \land  A \in K_J \backslash \{e\}
    \]
    \medskip

    Damit folgt, dass wir für jede Relation $K_{J_y}(e, k_1, \ldots, k_x), x \leq n$ auch den Eintrag in der Zelle $(K_{J_y}, k_x)$\footnote{\textit{(Zeile, Spalte)}} durch das Symbol $a$ mit entsprechendem Index ersetzen können, was für Zerlegungen bei binären Relationen $K_{J_i}(e, k_i)$ zu folgendem Tableau führt:

    \[
        \begin{array}{c|cccccc}
            & e      & k_1 & k_2 & k_3 & \dots  & k_n \\
            \hline
            K_{J_1}      & a_e     & a_1         & \_         & \_         & \dots  & \_         \\
            K_{J_2}      & a_e     & \_         & a_2         & \_         & \dots  & \_         \\
            K_{J_3}      & a_e     & \_         & \_         & a_3        & \dots  & \_         \\
            \vdots          & \vdots & \vdots     & \vdots     & \vdots     & \ddots & \vdots     \\
            K_{J_m}      & a_e     & \_         & \_         & \_         & \dots  & a_n
        \end{array}
    \]
    \medskip
    bei anderen Zerlegungen, die redundante Komponenten-Attribute aufweisen, wiederholt sich das $a$-Symbol in der Komponentenspalte der Relationszeile entsprechend.

    \noindent
    Damit ist das Tableau für den iterativen Teil des Verfahrens vorbereitet.\\

    \noindent
    Nun wird jede funktionale Abhängigkeit $X \rightarrow Y \in F^\text{min}_{K_J}$ iterativ betrachtet: Für alle Zeilen, die in den Spalten, die durch $X$ repräsentiert werden, einen gleichen Eintrag haben, werden die Spalten, die $Y$ repräsentieren, zeilenweise auf das gleiche Symbol gesetzt:\footnote{
        Da mit den gegebenen $F^\text{min}_{K_J}$ in jedem Fall so eine Spalte existiert, brauchen wir den anderen Fall nicht berücksichtigen: In diesem Fall würde die Spalte mit $b$-Symbolen gefüllt.
    }\\
    Ausgehend von der minimalen funktionalen Abhängigkeitsmenge $F^\text{min}_{K_J}$ können wir somit für jede $(e \rightarrow k_x) \in F^\text{min}_{K_J}, x \leq n$ die Spalte mit dem Index $x$ mit $a_x$-Symbolen füllen, da jedes in $K_J$ vorkommende Attribut von $e$ funktional abhängig ist.\\

    \noindent
    Nach Beendigung des Verfahrens ist das Tableau komplett mit $a$-Symbolen besetzt.
    \[
        \begin{array}{c|cccccc}
            & e      & k_1 & k_2 & k_3 & \dots  & k_n \\
            \hline
            K_{J_1}      & $a_e$     & $a_1$         & $a_2$         & $a_3$         & \dots  & $a_n$         \\
            K_{J_2}      & $a_e$     & $a_1$         & $a_2$         & $a_3$         & \dots  & $a_n$         \\
            K_{J_3}      & $a_e$     & $a_1$         & $a_2$         & $a_3$        & \dots  & $a_n$         \\
            \vdots          & \vdots & \vdots     & \vdots     & \vdots     & \ddots & \vdots     \\
            K_{J_n}      & $a_e$     & $a_1$         & $a_2$         & $a_3$         & \dots  & $a_n$
        \end{array}
    \]
    \medskip

    \noindent
    Da das Verfahren zum Nachweis der verlustfreien Zerlegung mindestens eine vollständig $a$-besetzte Zeile fordert, haben wir gezeigt, dass sich $K_J$ verlustfrei in die Relationen $K_{J_1}, K_{J_2}, K_{J_3}, \ldots, K_{J_m}$ zerlegen lässt.

\end{proof}
\bigskip


\begin{definition}\label{def:verlustfrei_redundanzfrei}
    Eine verlustfreie Zerlegung  $D = (R_1, R_2, \ldots, R_n)$ mit
    \[
        \njoin_{i=1}^n R_i = \pi_{R}(r)
    \]
    wird \textit{verlustfrei-redundanzfrei} genannt, wenn es keine Relation $R_j \in D$ gibt, mit der gilt:\footnote{
        Siehe \cite[Definition 7]{Nor98}.
    }
    \[
        \njoin_{i=1, i \neq j}^n R_i = \pi_{R}(r)
    \]

\end{definition}

\begin{lemma}\label{lem:verlustfrei_redundanzfrei_k}
    Jede verlustfreie Zerlegung von $K_J$
    \[
        D = (K_{J_1}, K_{J_2}, \ldots, K_{J_m})
    \]
    lässt sich auf einen verlustfrei-redundanzfreien Kern $D'$ reduzieren.
\end{lemma}
\begin{proof}
    Sei $D$ eine beliebige verlustfreie Zerlegung von $K_J$, sei $D' = D$.\\

    Falls $D'$ nicht verlustfrei-redundanzfrei ist, existiert mindestens ein $K_{J_q} \in D'$, so dass für jeden legalen Relationszustand $r$ von $K_J$ gilt:

    \[
        \njoin_{K_{J_i} \in D' \setminus \{K_{J_q}\}} \pi_{K_{J_i}}(r) = \pi_{K_J}(r)
    \]
    Jedes solche $K_{J_q}$ wird aus $D'$ entfernt.
    Der Schritt wird so lange wiederholt, bis $D'$ verlustfrei-redundanzfrei ist.
    Damit gilt für jedes $K_{J_q} \in D'$ mindestens ein legaler Relationszustand $r$ von $K_J$, so dass
    \[
        \njoin_{K_{J_i} \in D' \setminus \{K_{J_q}\}} \pi_{K_{J_i}}(r) \neq \pi_{K_J}(r)
    \]
    Es ist sichergestellt, dass das Verfahren terminiert, da $D$ endlich ist und mit jedem Schritt genau eine Relation aus $D'$ entfernt wird.
    Ist die Bedingung der verlustfreien Redundanzfreiheit verletzt, kann aber auch mindestens ein redundantes Element entfernt werden, ohne dass für jeden legalen Relationszustand $r$ von $K_J$
    \[
        \njoin_{K_{J_i} \in D'} \pi_{K_{J_i}}(r) = \pi_{K_J}(r)
    \]
    verletzt wird -- somit kann die Zerlegung auch nicht leer sein.
\end{proof}
\bigskip


\begin{lemma}\label{lem:k_abhaengigkeitserhalten}
    Jede entitätsbezogene Zerlegung $D$ von $K^\top$ ist abhängigkeitserhaltend.
\end{lemma}
\begin{proof}
    Zu zeigen ist, dass die Vereinigung der Projektionen von $F_{K^\top}$ auf jede $K^\top_i \in D$ äquivalent zu $F_{K^\top}$ ist:
    \[
        (\pi_{K^\top_1}(F_{K^\top}) \cup \pi_{K^\top_2}(F_{K^\top})  \cup \ldots \cup \pi_{K^\top_m}(F_{K^\top}))^+ = (F_{K^\top})^+
    \]
    \smallskip

    \noindent
    Für jede funktionale Abhängigkeitsmenge $F_{K^\top_x}$ einer Relation $K^\top_i \in D, i \leq m$, gilt:

    \[
        F_{K^\top_i} = \{e \rightarrow k^\top_i \mid k^\top_i \in K^\top_i \}
    \]
    \medskip
    \noindent
    Daraus können wir unmittelbar
    \[
        \pi_{K^\top_i}(F_{K^\top}) = F_{R_i}
    \]
    \noindent
    ableiten.\\

    \noindent
    Wegen
    \[
        \bigcup_{i=1}^{m} K^\top_i = K^\top
    \]
    sind alle Komponentenattribute aus $K^\top$ entlang den Relationen verteilt, womit keine funktionale Abhängigkeit mit einer Zerlegung $D$ von $K^\top$ verlorengeht.
    Damit gilt insgesamt

    \[
        (\bigcup_{i=1}^m F_{K^\top_i})^+ = (F_{K^\top})^+
    \]
\end{proof}
\bigskip

\bigskip
\begin{lemma}\label{lem:jede_K_zerlegung_ist_verlustfrei}
    Jede entitätsbezogene Zerlegung $D$ von $K^\top$ auf Basis der funktionalen Abhängigkeiten $F_{K^\top}$ ist verlustfrei.
\end{lemma}

\medskip
\begin{proof}
    Mit Lemma~\ref{lem:k_abhaengigkeitserhalten} haben wir nachgewiesen, dass jede entitätsbezogene Zerlegung $D$ abhängigkeitserhaltend bezüglich $F_{K^\top}$ ist.
    Damit ist sichergestellt, dass die für das Chase-Verfahren und den Aufbau des Tableaus benötigten funktionalen Abhängigkeiten zum Nachweis der Verlustlosigkeit nicht verloren gehen.\\

    \noindent
    Damit kann unter Anwendung des in Lemma~\ref{lem:K_plus_ist_verlustfrei} beschriebenen Verfahrens ein Tableau für eine Zerlegung wie folgt beschrieben werden: Die Zeilenbeschriftungen eines jeden solchen Tableaus bestehen aus den Relationen
    \[
        K^\top_x \in D,\quad \bigcup_{x=1}^m K^\top_x = K^\top
    \]
    \smallskip
    Die Spaltenüberschriften bestehen aus den Attributen
    \[
        e, k^\top_1, k^\top_2, \ldots, k^\top_n
    \]
    \medskip
    \noindent
    Bei der Anwendung des Verfahrens (siehe Beweis zu~\ref{lem:K_plus_ist_verlustfrei}) erhält nun jede Zeile in der $e$-Spalte ein $a$-Symbol, da mit der Definition einer entitätsbezogenen Zerlegung das Attribut in jeder Relation der Zerlegung vorkommt.
    Da jede Zeile ein $a$-Symbol in der $e$-Spalte aufweist, enthält jede Zeile $K^\top_x$ mit der funktionalen Abhängigkeit
    \[
        e \rightarrow k^{\top}_y, k^{\top}_y \in R_x
    \]
    in einer Zelle $(K^\top_x, k^\top_y), k^\top_y \in K^\top_x$ ein $a$-Symbol: Da alle funktionalen Abhängigkeiten und die Komponentenattribute ebenfalls vollständig in der Zerlegung enthalten sind, besitzt jede Spalte einer entsprechenden Zeile ein solches Symbol.
    Mit der Vorschrift, dass die linke Seite der FD in dem Verfahren Spalten zeilenweise vereinheitlicht -- in diesem Fall ausgehend von der $e$-Spalte als linke Seite einer jeden FD -- ist nach Beendigung des Verfahrens die hinreichende Bedingung erfüllt, dass zum Nachweis der verlustfreien Join-Eigenschaft mindestens eine Zeile nur aus aus $a$-Symbolen besteht.
    Tatsächlich besteht das Tableau dann ausschließlich aus $a$-Symbolen.
\end{proof}
\bigskip

\bigskip
\begin{lemma}\label{lem:verlustfrei-redundanzfrei-ist-entitätsbezogen}
    Jede verlustfrei-redundanzfreie Zerlegung von $K^\top$ ist eine entitätsbezogene Zerlegung.
\end{lemma}
\begin{proof}
    Sei $D = (K^\top_1, K^\top_2, \ldots, K^\top_m)$ eine verlustfrei-redundanzfreie Zerlegung von $K^\top$.
    Damit gilt
    \[
        \njoin_{i=1}^m(\pi_{K^\top_i}(r)) = \pi_{K^\top}(r)
    \]

    Zu zeigen:
    \[
        \forall K^\top_i: e \in K^\top_i
    \]
    Wir betrachten alle Relationen in $D$, die das Schlüsselattribut $e$ enthalten:
    \[
        D_e =\{K^\top_i \mid e \in K^\top_i \}, K^{\top,e}_{i} \in D_e
    \]
    \begin{itemize}
        \item $D = D_e$: Es ist nichts zu zeigen.
        \item $D \neq D_e$:
        \begin{itemize}
            \item Fall 1: Es gilt $\njoin_{i=1}^m(\pi_{K^{\top,e}_{i}}(r)) = \pi_{K^\top}(r)$.\\
            In dem Fall können alle Relationen aus $D \setminus D_e$ entfernt werden, ohne die verlustfreie Rekonstruktion von $K^\top$ zu verletzen.
            Dies widerspricht der Voraussetzung, dass $D$ verlustfrei-redundanzfrei ist.
            \item Fall 2: Es gilt $\njoin_{i=1}^m(\pi_{K^{\top,e}_{i}}(r)) \neq \pi_{K^\top}(r)$.\\
            Über die Zerlegung $D_e$ kann kein legaler Relationszustand von $K^\top$ hergestellt werden, womit sie nicht verlustfrei ist.
            Da $D_e$ nicht verlustfrei ist und nach Lemma~\ref{lem:jede_K_zerlegung_ist_verlustfrei} jede entitätsbezogene Zerlegung (Definition~\ref{def:entitätsbezogen}) auch verlustfrei ist, ist die Zerlegung auch nicht entitätsbezogen.\\
            Damit gilt
            \[
                \bigcup_{K^\top_j \in D_e} K^\top_j \neq K^\top
            \]
            was eine fehlende Attributüberdeckung der Relationen in $D_e$ impliziert.\\

            \noindent
            Damit gibt es mindestens ein Komponentenattribut in einer Relation der Zerlegung $D \setminus D_e$ mit
            \[
                \exists k^\top_x \in K^\top_j, K^\top_j \in D \setminus D_e
            \]
            Bei der Anwendung des Chase-Verfahrens verbleibt damit ein $b$-Symbol bei der Attributspalte $e$ in der zugehörigen Zeile $K^\top_j$ und verhindert damit die Vereinheitlichung der Zeile mit den übrigen Zeilen, die ein $a$-Symbol in der $e$-Spalte besitzen:
            Dass dieses Attribut nicht in den Relationen von $D_e$ vorkommt, sondern in mindestens einer der Relationen von $D \semtinus D_e$, bedeutet, dass das Chase-Verfahren in diesem Kontext keine Zeile vollständig mit $a$-Symbolen besetzen kann, solange nicht auch $e$ ein Attribut der Relationen von $D \setminus D_e$ ist, und im Chase-Verfahren ein $a$-Symbol erhält.
            Im Widerspruch zur Annahme ist die Zerlegung also nicht verlustfrei.
        \end{itemize}
    \end{itemize}



\end{proof}

\noindent
Aufbauend auf den vorangehenden Hilfssätzen können wir nun auf die JOIN-Abhängigkeiten (JD) für eine Zerlegung von ${K^\top}$ eingehen.

\bigskip
\begin{corollary}\label{cor:k_joinabhaengigkeiten}
    Jede verlustfrei-redundanzfreie Zerlegung von $K^\top$ induziert eine JOIN-Abhängigkeit entitätsbezogener Relationen
    \[
        \text{JD}(K^\top_1, K^\top_2, \ldots, R_m),\quad \text{mit } K^\top_1 \cup K^\top_2 \cup \ldots \cup K^\top_m = K^\top
    \]
    \noindent
    bezüglich $K^\top$
\end{corollary}
\begin{proof}
    Zu zeigen ist, dass
    \[
        \njoin\ (\pi_{K^\top_1}(r), \pi_{K^\top_2}(r), \ldots, \pi_{K^\top_m}(r)) = r
    \]
    \noindent
    für jeden Zustand $r$ von $K^\top$ gilt, und dass jede Relation der Zerlegung entitätsbezogen ist.\\

    \noindent
    Da die Zerlegung nach Voraussetzung verlustfrei ist, induziert sie eine Join-Abhängigkeit.\\
    Mit Lemma~\ref{lem:verlustfrei-redundanzfrei-ist-entitätsbezogen} ist die Zerlegung auch entitätsbezogen.
\end{proof}
\bigskip

\begin{tcolorbox}[excursusbox, title={Definition der 5. Normalform in der Literatur}]
    Die {5.} Normalform geht zurück auf Fagin, der sie 1979 mit~\cite{Fag79} als \textit{Project-Join-Normalform} (\textit{PJ/NF}) einführt.
    In Anlehnung daran wird sie von Vossen wie folgt angegeben:
    \begin{displayquote}
        Ein Relationenschema $R$ ist in \textit{fünfter Normalform} (5NF) (bzw. in \textit{Project-Join-Normalform}, PJ/NF), falls jede JD von $R$ allein von den Schlüsselabhängigkeiten von $R$ impliziert wird.
        \par\hfill\cite[Definition 8.12, S. 273, Hervorhebung i.O.]{Vos08}
    \end{displayquote}

    Diese Definition findet sich ebenfalls in der Arbeit von Darwen et {al.}~\cite[Definition 2.3]{Dar12}.
    Dort gehen die Autoren auch auf inkorrekte Definitionen der 5NF ein, wie die folgende von einem der Mitautoren:

    \begin{displayquote}
        A relvar $R$ is in 5NF -- also called project-join normal form (PJ/NF) -- if, and only if, every nontrivial join dependency that is satisfied by $R$ is implied by the candidate key(s) of $R$, where:
        \begin{itemize}
            \item[] a. The join dependency $\{A, B, \ldots, Z\}$ on $R$ is trivial if and only if at least one of $A, B, \ldots, Z$ is the set of all attributes of $R$.
            \item[] b. The join dependency $*\{A, B, \ldots, Z\}$ on $R$ is implied by the candidate key(s) of $R$ if and only if each of $A, B, \ldots, Z$ is a superkey for $R$.
        \end{itemize}
        \par\hfill\cite[S. 389--390]{Dat03}
    \end{displayquote}

    In ähnlicher Form findet sich diese Definition auch bei Elmasri und Navathe:
    \begin{displayquote}
        A relation schema $R$ is in \textbf{fifth normal form} (\textbf{5NF}) (or \textbf{project-join normal form} (\textbf{PJNF})) with respect to a set $F$ of functional, multivalued, and join dependencies if, for every nontrivial join dependency $\text{JD}(R_1, R_2, \ldots, R_n)$ in $F^+$ (that is, implied by $F$), every $R_i$ is a superkey of $R$.
        \par\hfill~\cite[S. 494, Hervorhebung i.O.]{EN16}
    \end{displayquote}

    Die Autoren merken an, dass die so formulierte Definition eher der \textit{Superkey Normal Form}-Definition entspricht (\textit{SKNF}).\footnote{
        ``[\ldots] many textbooks [\ldots] incorrectly give the deﬁnition of SKNF as a deﬁnition of 5NF.`` (ebd.)
    }, was Normann auch in~\cite{Nor98} diskutiert.\\

    Darwen et {al.} zeigen in der zitierten Arbeit, dass die SKNF nicht äquivalent zu der 5NF ist -- sie ist sogar schwächer.
    Im Kontext dieser Arbeit nehmen wir deshalb für die 5NF implizit die Definition von Fagin beziehungsweise Vossen an.
\end{tcolorbox}
\bigskip


Nun kann der Nachweis für die Normalformen erfolgen.
\medskip



\begin{theorem}\label{f_min_norm}
    Das Relationsschema $K^\top$ erfüllt die Bedingungen der 1., 2., 3., 4. und 5. sowie der Boyce-Codd-Normalform.
\end{theorem}

\begin{proof}
    \medskip
    {\ }
    \begin{itemize}
        \item 1. Normalform: Erfüllt, da mit Axiom~\ref{ax:nur_atomar} alle Attribute in $K^\top$ als atomare Attribute definiert sind.
        \item 2. Normalform: Erfüllt, da $K^\top$ das Attribut $e$ als alleinigen Schlüssel enthält und alle Attribute voll funktional von $e$ abhängig sind.
        \item 3. Normalform: Erfüllt, da $K^\top$ keine transitiven Abhängigkeiten enthält: Alle Nicht-Prime-Attribute sind funktional voll abhängig von dem einzigen Prime-Attribut $e$.
        \item BCNF: Erfüllt, da für jede nicht-triviale funktionale Abhängigkeit $(X \rightarrow A)\ \in (F^\text{min}_{K^\top})^+$ die linke Seite der FD der Superschlüssel von $K^\top$ ist.
        \item 4. Normalform: Zu zeigen ist, dass $X$ für jede nichttriviale mehrwertige Abhängigkeit $X \twoheadrightarrow Y \in (F^{\text{min}}_{K^\top})^+$ ein Superschlüssel von $K^\top$ ist.\\[2mm]
        Sei hierzu $C_K^\top \coloneqq \{k^\top_1, k^\top_2, \ldots, k^\top_n \}$.\\[2mm]
        Wir betrachten im Folgenden alle aus $F^\text{min}_{K^\top}$ induzierten mehrwertigen Abhängigkeiten der Form
        \[
            e \twoheadrightarrow Y,\qquad \text{mit}\ \emptyset \neq Y \subset (K^\top \setminus \{e\})
        \]
        \noindent
        Mit der Voraussetzung $n \geq 2$ sind diese Abhängigkeiten nichttrivial, da
        \[
             Y \nsubseteq \{e\}
        \]
        und
        \[
            \{e\} \cup Y \neq K^\top
        \]
        gilt.\\
        Jede funktionale Abhängigkeit in $F^\text{min}_{K^\top}$ ist auch eine nichttriviale mehrwertige Abhängigkeit~\cite[S. 353]{EN11}.\\
        In jeder MVD ist der Primärschlüssel $X=e$ das einzige Prime-Attribut auf der linken Seite.
        Damit ist die geforderte Bedingung insgesamt erfüllt.
        \item 5. Normalform: Zu zeigen ist, dass jede JD von $K^\top$ allein von den Schlüsselabhängigkeiten von $K^\top$ impliziert wird.\\
        \begin{enumerate}
            \item Mit Lemma~\ref{lem:verlustfrei_redundanzfrei_k} haben wir gezeigt, dass jede Zerlegung von $K^\top$ einen verlustfrei-redundanzfreien Kern besitzt.
            Diesen Kern bezüglich JD bezeichnen wir im Folgenden mit $D^{JD}$.
            Die ursprüngliche JD beinhaltet im Vergleich zu $JD^{JD}$ per Definition~\ref{def:verlustfrei_redundanzfrei} nur redundante Relationen, die keine zusätzlichen Zerlegungsinformationen tragen, weshalb es ausreicht, nur den Kern zu betrachten.
            \item Mit Lemma~\ref{lem:verlustfrei-redundanzfrei-ist-entitätsbezogen} haben wir gezeigt, dass eine solche Zerlegung aus entitätsbezogenen Relationen $K^\top_i$ besteht.
            \item So gilt ebenfalls für $D^{JD}$:
            \[
                \forall K^\top_i \in D^{JD}: e \in K^\top_i
            \]
        \end{enumerate}

        Damit enthält enthält der verlustfrei-redundanzfreie Kern $D^{JD}$ ausschließlich Relationen, die den Primärschlüssel $e$ mit $e^+ = K^\top$ enthalten.
        Damit folgt insgesamt, dass jede JD allein von den Schlüsselabhängigkeiten von $K^\top$ impliziert wird.\footnote{
        Alternativ folgt die 5NF auch aus~\cite[Theorem 4.1]{DF92}, falls $K^\top$ in 3NF ist, und jeder Schlüssel ein einfacher Schlüssel ist.
        Dass $K^\top$ diese Bedingungen erfüllt, ergibt sich aus dem Text.
        }

    \end{itemize}
\end{proof}
\bigskip



