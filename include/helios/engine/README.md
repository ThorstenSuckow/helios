## helios::engine

This namespace provides core engine utilities for frame timing and update lifecycle management. It includes classes for frame pacing and measurement, and interfaces for objects that need per-frame updates.

---
<details>
<summary>Doxygen</summary><p>
@namespace helios::engine
@brief Engine utilities for frame pacing and tick/update lifecycle management.
@details This namespace provides core engine utilities for frame timing and update lifecycle management. It includes classes for frame pacing and measurement (FramePacer, FrameStats, FPS metrics), and the Tickable interface for objects that are updated every frame.
</p></details>

