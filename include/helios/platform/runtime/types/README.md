# helios::platform::runtime::types

Strong ids and handles for runtime platform entities.

## Types

| Type | Purpose |
|------|---------|
| `PlatformId` | Strong id type for runtime platform entities |
| `PlatformHandle` | ECS entity handle type for runtime platform entities |

---
<details>
<summary>Doxygen</summary><p>
@namespace helios::platform::runtime::types
@brief Runtime platform id and handle types.
</p></details>

