# helios::rendering::mesh::commands

Mesh command payload types.

| Command | Description |
|---------|-------------|
| `MeshRenderCommand` | Per-draw payload with render prototype and uniform maps |

---
<details>
<summary>Doxygen</summary><p>
@namespace helios::rendering::mesh::commands
@brief Mesh command payload types.
@details Contains command objects consumed by mesh renderers and render queues.
</p></details>

