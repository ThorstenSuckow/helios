# helios::rendering::shader::types

Strong id and handle aliases for shader resources.

| Type | Description |
|------|-------------|
| `ShaderIdTag` | Tag type for strong-id specialization |
| `ShaderId` | Strong id for shader resources |
| `ShaderHandle` | ECS entity handle bound to `ShaderId` |

---
<details>
<summary>Doxygen</summary><p>
@namespace helios::rendering::shader::types
@brief Strong id and handle aliases for shader resources.
@details Defines the identity and handle layer used by shader entity registries and managers.
</p></details>

