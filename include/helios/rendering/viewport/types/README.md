# helios::rendering::viewport::types

Strong id and handle aliases for viewport resources.

| Type | Description |
|------|-------------|
| `ViewportIdTag` | Tag type for strong-id specialization |
| `ViewportId` | Strong id for viewport resources |
| `ViewportHandle` | ECS entity handle bound to `ViewportId` |

---
<details>
<summary>Doxygen</summary><p>
@namespace helios::rendering::viewport::types
@brief Strong id and handle aliases for viewport resources.
@details Defines identity and handle types for viewport ECS registries and managers.
</p></details>

