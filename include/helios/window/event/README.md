# helios::window::event

This namespace provides window-specific event types such as resize events and framebuffer events for handling window state changes. Defines event types for communicating window state changes to the application, enabling responsive handling of window operations.

---
<details>
<summary>Doxygen</summary><p>
@namespace helios::window::event
@brief Window event definitions.
@details This namespace provides window-specific event types such as resize events and framebuffer events for handling window state changes. Defines event types for communicating window state changes to the application, enabling responsive handling of window operations.
</p></details>
