/**
 * @file _module.ixx
 * @brief Module exports for window event types and handlers.
 */

/**
 * @brief Window event definitions and handlers.
 *
 * @details This module provides window-specific event types such as resize events
 * and framebuffer events for handling window state changes.
 */
export module helios.window.event;

export import helios.window.event.WindowEvent;
export import helios.window.event.FrameBufferResizeEvent;
